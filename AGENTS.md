# SAKURA 导航仓库维护规范

## 作用域与优先级

本文件适用于整个仓库。当前用户任务中的明确要求优先于本文件；但不得擅自放宽准确查重、目标确认、安全核实、必要验证、不提交无关文件和未经允许不推送等规则。

## 仓库基本规则

- 本项目使用原生 HTML、CSS、JavaScript 前台，以及 Cloudflare Worker + D1 管理后台；不引入前端框架。
- 生产环境导航数据以 D1 为准，通过 `/admin/` 管理；`assets/js/sites-data.js` 是公开页面在 API 不可用时的随仓库快照，也是首次 D1 种子数据的来源。
- 普通网站或板块的生产内容优先通过管理后台修改。明确要求修改仓库数据时，必须同步快照与对应的增量 migration；不得把已经应用的历史 migration 改写成新的生产状态。
- 板块新增、删除、改名或修改 ID 还必须检查 Worker 校验、D1 外键、前台逻辑、验证脚本和本文件。
- 执行任务前先确认仓库为 `XHSCF/sakura-nav`、当前分支为 `main`，并检查工作区状态。
- 公开页面统一使用品牌名“SAKURA导航”，不得展示 GitHub 仓库地址、仓库链接或“Cloudflare 自动部署”文字；仓库与部署信息只保留在维护文档中。
- 保留用户或其他任务产生的无关修改；不覆盖、还原、暂存或提交它们。
- 不擅自重构、格式化、重排、清理或顺手优化无关代码，不扩大任务范围。
- 不创建新分支或 Pull Request。未经用户明确要求，不执行 `git push` 或其他远程写入。
- 修改完成并验证通过后只创建本地提交，由用户通过 GitHub Desktop 推送。
- 没有实际修改时不创建空提交。
- 不提交 `link-health-report.md`、缓存、临时文件或其他自动生成文件。

## 当前板块对应关系

- 在线影视 → `movies` → `fa-film`
- 动漫番剧 → `anime` → `fa-play-circle`
- 资源下载 → `downloads` → `fa-cloud-download-alt`
- iOS专区 → `ios` → `fa-mobile-alt`
- 安卓专区 → `android` → `fa-robot`
- PC专区 → `software` → `fa-laptop-code`
- 在线工具 → `tools` → `fa-tools`
- 游戏专区 → `games` → `fa-gamepad`

旧的 `ppt` 分类参数仅作为 `software` 的 URL 兼容别名保留；新增或修改网站数据时必须使用 `software`，不得继续写入 `ppt`。

## 管理后台与 D1

- 后台固定入口为 `/admin/`；公开页面不得增加明显的后台入口、管理员账号提示或部署信息。
- Worker 业务源码位于 `worker/index.mjs`，D1 migration 位于 `migrations/`，后台静态页面位于 `admin/`。`wrangler.jsonc` 中的绑定名称固定为 `ASSETS` 和 `DB`。
- 管理员账号、密码和会话签名密钥只能通过 Cloudflare Secrets 或未跟踪的 `.dev.vars` 提供，变量名分别为 `ADMIN_USERNAME`、`ADMIN_PASSWORD`、`ADMIN_SESSION_SECRET`；禁止把真实值、哈希、Token、Cookie 或数据库备份提交到仓库、日志、截图或回复中。
- 管理员会话必须使用 HttpOnly、SameSite Cookie 和 HMAC 签名；所有写操作必须校验登录状态、同源 Origin 与 CSRF Token。登录失败必须限速，不得把具体账号是否存在返回给客户端。
- API 必须限制请求体大小、验证 ID、字段长度、HTTP(S) URL、日期、分类引用、单/双按钮组合以及重复名称和链接；不得因为后台表单存在就放宽本文件的卡片数据规则。
- 公开接口只返回已发布的可见分类和普通卡片。隐藏板块网站只能在口令通过服务端校验后返回；公开数据接口不得返回隐藏口令、管理员字段、草稿、修改记录或登录信息。
- 后台支持草稿和发布。草稿不得出现在公开首页、公开搜索、分类数量、最近访问或隐藏板块中。
- 删除卡片必须二次确认；删除非空分类必须由服务端拒绝。排序更新必须保持 ID 稳定，不得通过删除并重建对象实现。
- 后台备份导出包含完整生产数据和隐藏设置，视为敏感维护文件，不提交 Git。导入前必须二次确认并完整执行本地字段与重复校验。
- 卡片编辑器必须提供与前台单双按钮布局一致的实时预览，并在名称或两行描述预计会被截断时给出清晰提示；提示用于帮助精简内容，不得绕过服务端字段校验。
- 卡片、分类和新世界设置发生修改后必须显示“尚未保存”状态。关闭弹窗、按 `Escape`、点击弹窗外部、离开设置页、退出后台或关闭页面时不得静默丢失修改；成功保存或明确确认放弃后才清除未保存状态。
- 卡片编辑弹窗的桌面双列字段必须保持等宽、等高和统一间距，聚焦状态不得用向外扩张的粗光圈造成控件大小不一；手机改为单列，平板保持有足够宽度的双列。弹窗打开时锁定背景页面滚动，只保留一条细窄的弹窗内容滚动条。
- 后台左侧导航固定按“卡片管理 / 分类管理 / 访问统计 / 修改记录 / 设置与备份”排列；修改记录必须紧邻访问统计并位于设置与备份之前，手机、平板和电脑保持相同逻辑顺序。
- 卡片编辑器按“基本信息、展示与发布、按钮设置、搜索与预览”划分清晰区域；字段标题、控件、帮助文字和相邻字段使用统一垂直节奏。“卡片位置”标题必须独立位于选择框上方并保留清晰间距，不得使用会让 `legend` 贴住或压在线框上的布局。
- 弹窗滚动锁定同时使用显式页面状态类作为 `:has()` 的兼容回退；弹窗外壳不得产生第二条滚动条，背景页面不得随触摸或滚轮移动，只有 `.dialog-scroll` 可以纵向滚动，最后一项、实时预览和底部保存栏不得互相遮挡。
- 新卡片 ID 继续允许保存前手动修改并在创建后保持稳定。自动生成时优先使用有意义的名称英文；名称只留下 1 至 3 个英文字母或 `app`、`web`、`site` 等通用短词时，应改用网址域名生成，避免“发现TV”变成 `tv`；纯数字品牌仍可保留数字 ID，重复时追加稳定的数字后缀。
- `input[type="date"]` 必须放在负责边框、圆角和宽度约束的 `.date-control` 外壳中，外壳裁切 iOS/iPadOS Safari 原生日期控件的固有宽度，确保手机、平板和电脑均不越过表单或弹窗右边界。
- 后台首屏必须先显示独立的会话验证加载态，登录页和后台主体均保持隐藏；只有 `/api/admin/session` 明确失败后才显示登录页，会话有效且后台数据加载完成后才显示后台主体，避免刷新时闪现登录表单或空后台。
- `migrations/0001_admin_schema.sql` 与 `0002_seed_navigation_data.sql` 一旦用于生产初始化即视为不可变历史。以后修改数据库结构或通过仓库发布内容变更时新增编号 migration，不回写旧 migration。
- `tools/generate_d1_seed.js` 只用于首次上线前从 `sites-data.js` 重新生成初始种子。生产后台启用后，不得用它覆盖已包含后台修改的数据。
- 前台通过 `assets/js/data-loader.js` 优先读取 D1；API 返回错误、超时或空数据时必须安全回退到 `sites-data.js`，保留现有搜索、筛选、按钮、最近访问、主题和响应式交互。
- 前台匿名访问统计只在首页和关于页加载完成后异步上报，不统计后台和 404 页面，不得阻塞首屏、导航数据或卡片交互。前端使用随机匿名编号，Worker 只保存其 SHA-256 哈希、访问时间、页面路径、来源域名、派生的设备/浏览器/系统，以及 Cloudflare 提供的国家、省/州和城市；禁止保存原始 IP、完整 User-Agent、经纬度、搜索内容、隐藏口令或其他可识别个人的信息。
- 匿名访问上报必须尊重 Global Privacy Control 与 Do Not Track，忽略站内跳转来源和常见机器人，限制请求体和字段长度，拒绝后台/API路径，并按匿名访客、页面和分钟去重。统计开关关闭后不得新增记录；访问记录默认保留 90 天并由 Cloudflare Cron 每日清理。
- 访问统计后台接口必须校验管理员会话，开关和清空操作还必须校验同源与 CSRF；清空全部访问记录前必须二次确认。后台按今天、7 天、30 天和 90 天显示访问次数、匿名访客、页面、设备、来源、国家与大致地区和最近记录，未知或缺失位置必须安全回退，不得猜测。
- 修改 Worker、D1、后台页面或数据加载逻辑时，必须运行 `node --test tools/test_worker.js`、`python tools/validate_admin.py`、现有前端测试和站点验证，并进行后台登录、CRUD、备份以及手机、平板、电脑的明暗模式检查。

## 主题配色系统

- 网站同时支持明暗模式与独立配色选择，两者不得互相替代：明暗模式继续使用 `sakura-theme`，配色使用 `sakura-color-theme` 保存在 localStorage。
- 当前配色固定包含：初音绿 `miku`（`#39C5BB`）、经典紫 `purple`、海洋蓝 `ocean`、苹果绿 `apple`、樱花粉 `sakura`、琥珀橙 `amber`、黑金色 `black-gold`、青绿色 `teal`。
- 浅色模式统一使用灰蓝色 `#E4EEF4` 作为页面、浏览器主题色和 PWA 启动画面背景；卡片保持白色或半透明白色，深色模式背景继续使用 `#171A24`。
- 初音绿是新用户、无有效保存值以及清除网站数据后的默认配色；保存过其他合法配色时，刷新页面、重新进入网站和切换明暗模式均须保留用户选择。
- 配色入口在所有公开页面的顶部操作区显示；选择面板必须支持鼠标、触摸、键盘聚焦、`Escape` 关闭、点击外部关闭，并正确标明当前选项。
- 管理后台顶部同时提供明暗模式和完整配色入口，与前台共用 `sakura-theme` 和 `sakura-color-theme`；后台选择后刷新、重新登录和返回前台均须保留并同步，配色面板继续满足鼠标、触摸、键盘与关闭交互要求。
- 配色通过 `data-color-theme` 和全局主题变量统一驱动分类按钮、卡片按钮、分类图标、边框、阴影、背景氛围及 Liquid Glass 高光；不得为不同板块或单张卡片硬编码独立配色。
- 网站分类栏使用 iOS 式分段选择器：外层为单一玻璃轨道，只保留四周等宽的细高光边缘，不得使用只增强顶部的非对称内高光，也不得使用会在轨道下方连成横向色带的大面积外阴影；轨道只能有一个位于文字下方的主题色玻璃指示胶囊，未选项保持透明，切换时指示胶囊以约 240ms 平滑横移并适应目标按钮宽度，按钮文字、数字和按钮本身不得跳跃、缩放或产生布局位移。手机和平板横向分类继续自动居中所选项，横向滚动、窗口缩放和字体加载后必须保持指示胶囊对齐；启用 `prefers-reduced-motion` 时直接切换位置，不播放移动动画。
- 首页搜索框与分类栏使用 Telegram 式紧凑层级：两者保持相同最大宽度、相近材质和较小垂直间距，搜索框在上、分类栏在下；分类栏外层统一约 46px 高并明显细于电脑 56px、平板 54px、手机 50px 的搜索框，内部分类按钮仍保留约 40px 点击高度。不得在两者之间插入站点总数或搜索说明。动态的“站点 / 分类”或筛选结果摘要固定居中显示在分类栏下方，不再显示“支持名称、描述、分类、缩写与多关键词搜索”。页面滚动时搜索框随页面离开，只有分类栏继续吸顶；手机、平板和电脑保持相同顺序，手机分类继续横向滑动。
- 首页不显示“全部站点 / 最近收录 / 最近访问”视图切换栏；分类栏中的“全部”按钮必须保留。顶部菜单固定按“首页 / 最近访问 / 关于”排列，最近访问使用 `?view=history`，点击首页恢复全部网站。旧的 `?view=recent` 必须安全回退首页并清理无效参数；删除最近收录视图不得删除网站 `addedAt`、卡片 `NEW` 标记或后台收录日期数据。
- 分类按钮与板块标题中的网站数量统一使用 22px 高的小型胶囊计数器：设置稳定的最小宽度、等宽数字和严格的水平垂直居中，个位数与多位数只允许胶囊宽度自然增加，不得变成大小不一的圆点、挤压文字或在移动端被裁切。选中状态只改变主题配色，不得改变计数器尺寸。
- 页面主题氛围渐变必须由连续的页面背景绘制，不得使用在首屏边界被裁切的伪元素制造手机或平板可见的横向分层色带。
- 新增、删除、改名或调整配色时，必须同步更新 `assets/js/theme-init.js` 的首屏初始化白名单、`assets/js/sakura-core.js` 的配色清单、`assets/css/sakura.css` 的浅深色变量、前端测试和本节说明，避免首屏闪色或刷新后丢失。

## 浏览器兼容与渐进增强

- 前台和管理后台必须采用渐进增强：现代浏览器继续显示 Liquid Glass、连续圆角和主题混色等高级效果；不支持相关能力的安卓厂商浏览器仍须显示结构、文字、背景、边框、图标底板、分段选择器和单双按钮均完整可辨的基础样式。
- 页面基础可用样式不得只依赖 `color-mix()`、`backdrop-filter`、`-webkit-backdrop-filter` 或 `corner-shape`。使用这些能力时必须保留普通颜色、实体背景、`border-radius` 等基础声明，并通过 `@supports` 提供必要回退；单条包含不受支持函数的声明被浏览器丢弃后，不得造成透明控件、消失的胶囊、无底板图标或不可见焦点。
- 前台和后台的根页面必须固定 `-webkit-text-size-adjust: 100%` 与 `text-size-adjust: 100%`，避免安卓浏览器自动放大局部文字后破坏卡片、按钮和表单对齐。
- 兼容整改不得按安卓品牌硬编码 User-Agent 或设备型号，不为不同厂商维护独立布局；统一按 CSS 能力检测降级，并保持相同 HTML 与交互逻辑。
- 修改前台或后台视觉样式时，除现代浏览器的手机、平板、电脑及浅色、深色检查外，还必须检查禁用 `color-mix()` 或玻璃模糊后的兼容基线，确认 320px 宽页面无横向溢出，分类指示器、卡片图标、名称、两行描述和按钮均正常显示。
- 首页普通板块标题、普通卡片和新世界卡片使用一次性滚动揭示：首次进入视口时以约 16px 上移、620ms 柔和淡入完成，同一对象在搜索或筛选重绘后不得反复播放；仅首批同组卡片允许最多 100ms 的轻微错峰。顶部导航、主题控件、搜索框、分类栏、提示和工具按钮保持稳定，不参与滚动揭示。
- 滚动揭示必须通过单个 `IntersectionObserver` 按需触发并及时取消观察，不得为每张卡片创建独立滚动监听。手机、平板和旧安卓不使用模糊滤镜；不支持 `IntersectionObserver` 或启用 `prefers-reduced-motion` 时直接显示完整内容，禁止因动画能力缺失产生空白板块或不可操作卡片。

收录网站时必须使用用户指定板块，不自行改换或创建分类，也不擅自修改分类名称、ID、图标或顺序。用户未指定板块且无法可靠判断时，停止修改并请用户确认。板块新增、删除、改名或修改 ID 后，必须同步更新本节，并确保它与 `assets/js/sites-data.js` 的分类数组一致。

网站卡片通过网站对象的 `category` 查找对应分类对象的 `icon`。同一分类下的所有卡片与板块标题使用同一个本地 Font Awesome Solid 图标，不同分类使用各自已配置的图标；分类缺少有效图标时，卡片使用本地 `fa-link` 安全回退。

## 新世界隐藏板块

仓库包含一个搜索口令触发的彩蛋板块。生产配置位于 D1 的 `settings` 与隐藏卡片记录中，`assets/js/sites-data.js` 的 `hiddenSection` 仅作为随仓库快照：

- ID：`new-world`
- 名称：新世界
- 图标：`fa-door-open`
- 入口口令：开门
- 欢迎词：欢迎踏入新世界的大门

该板块不是普通 `categories` 项，也不得加入普通分类按钮。只有在搜索框输入与口令完全匹配的内容时才进入；口令不得写入 URL、localStorage 或最近搜索记录。进入后只显示新世界、欢迎词、访问风险提示、隐藏网站和退出按钮；退出按钮与 `Escape` 必须恢复正常页面，刷新页面不得自动保持隐藏状态。

隐藏网站在生产环境中存放为 D1 的 `is_hidden = 1` 卡片；随仓库快照仍使用 `hiddenSection.sites`，对象格式如下：

```js
{ id: "example", name: "网站正式名称", url: "https://example.com/", description: "一句简短准确的中文描述。", keywords: ["名称", "用途"] }
```

- 收录隐藏网站时，以用户明确提供的网址、名称和描述为准；视为用户已自行核实，不访问目标网站，不进行联网可用性、跳转、正式名称或内容复核，也不运行针对该网址的链接健康检查。
- 隐藏网站仍须在仓库本地检查必填字段、URL 基本格式以及 ID、名称和 URL 是否与普通或隐藏网站重复；用户未提供网址、名称或描述时应先请用户补充，不得猜测。
- 隐藏网站只有单按钮和双按钮两种卡片形态。没有 `urlLabel` 时自动成为单按钮卡片，固定显示“点击进入”并使用 `url`；也可使用 `urlLabel`、`secondaryUrl`、`secondaryUrlLabel` 成为双按钮卡片。提供第二个 URL 时三个扩展字段必须完整，按钮名称和 URL 不得重复。所有隐藏卡片主体均不可点击，只能通过卡片内按钮跳转。
- 隐藏卡片与普通卡片使用同一套 136px 横向布局和两行描述规则；单按钮位于右侧垂直居中，双按钮在手机、平板和电脑上均位于右侧上下等宽排列，不得恢复为并排按钮或图标、文字、按钮上下散落的旧布局。
- 隐藏网站不得添加 `category`、`icon`、`addedAt` 或 `recent` 字段，全部自动继承新世界的门图标。
- 隐藏网站卡片内不显示“新世界”分类文字；所属板块已由顶部标题明确展示，不在每张卡片中重复提示。
- 隐藏网站不得出现在普通首页、普通搜索、分类数量或最近访问中，也不得影响页脚“导航数据更新于”的日期。
- 隐藏网站不写入最近访问 localStorage，单按钮和双按钮点击均不得记录访问；普通网站收录不得擅自加入隐藏板块，只有用户明确指定“新世界”时才允许修改 `hiddenSection.sites`。
- 欢迎词和访问风险提示是该板块的固定入口内容，未经用户明确要求不得删除或改写。
- 这是公开静态站中的视觉彩蛋，不是身份验证或安全权限；禁止在其中保存密码、Token、私人资料或其他敏感内容。
- 回归测试不得硬编码隐藏网站的完整名单或数量；应动态检查固定板块配置、网站字段结构、ID/URL 唯一性、禁止字段和关键交互，确保以后新增或删除隐藏网站时不需要同步改写测试名单。
- 修改隐藏板块、口令或隐藏网站后，必须检查精确口令、近似词不误触发、URL 不泄露口令、退出恢复、刷新退出、空板块和含卡片状态，并完成手机、平板、电脑及浅色、深色回归测试。

## 收录新网站

### 核实与查重

新增前必须核实：

- 网站当前可访问性、正式名称和主要用途。
- HTTP 是否跳转到 HTTPS，是否发生域名跳转，以及最终稳定网址。
- 网址是否包含可删除的无意义追踪参数。
- 仓库是否已有相同网站、相同域名、旧域名、镜像域名或最终跳转地址。
- 名称相似的网站是否确实为不同服务。

若已收录，不重复添加、不修改原条目、不创建空提交，并向用户说明现有名称、网址和分类。若域名停放、跳转到无关内容、完全无法核实或存在明显恶意、诈骗或安全风险，不添加、不提交并说明原因。

不能仅凭单次出现 403、Cloudflare 验证、反爬、地区限制、超时、TLS/证书异常或 HEAD 被拒绝认定网站失效；这些情况需要谨慎复核。

### 数据格式

通过后台录入时遵循相同字段契约；明确修改仓库快照时继续遵循 `assets/js/sites-data.js` 当前缩进、顺序和代码风格。基本字段为：

```js
{
  id: "example",
  name: "网站正式名称",
  url: "https://example.com/",
  description: "一句简短准确的中文描述。",
  category: "tools",
  keywords: ["网站名称", "英文名称", "域名核心词", "主要用途"],
  addedAt: "YYYY-MM-DD"
}
```

- `id`：简短、唯一、稳定，只使用英文小写字母、数字和连字符。
- `name`：使用当前正式名称，不添加“官网”“最新地址”等宣传词。
- `url`：使用核实后的稳定地址，优先 HTTPS，删除无意义追踪参数。
- `description`：一句简短、客观的中文说明，风格与现有条目一致；必须确保在手机、平板和电脑卡片的两行描述区域内都能完整显示，优先删减与名称重复的内容、冗余修饰和非必要说明，不得依赖省略号隐藏内容。
- `category`：严格使用用户指定板块对应的分类 ID。
- `keywords`：包含名称、常用缩写、域名核心词和主要用途，不堆砌无关词。
- `addedAt`：使用实际收录日期，必须是合法的 `YYYY-MM-DD`；页脚“导航数据更新于”会自动读取全部条目中最新的合法日期。
- 当前网站只有单按钮卡片和双按钮卡片两种形态。没有 `urlLabel` 的基础网站对象默认渲染为单按钮卡片，按钮名称固定为“点击进入”，地址使用当前对象的 `url`；不得为了单按钮另增按钮名称字段。
- 用户明确指定某张卡片使用两个按钮时，保留 `url` 作为第一个按钮地址并填写 `urlLabel`。用户提供第二个 URL 时，必须同时填写 `secondaryUrl` 和 `secondaryUrlLabel`，实际两个按钮名称和两个 URL 均不得重复。
- 用户只提供一个 URL 时，不猜测或补充第二个外部地址，也不写入 `secondaryUrl`、`secondaryUrlLabel`；第二个按钮由前端自动显示为“暂无”并指向站内 `./404.html`。若用户为无 URL 的第二按钮提供了名称，仍统一使用“暂无”。
- 所有卡片主体均不可点击，只能点击内部按钮跳转；按钮均在新标签页打开。普通网站的单按钮和双按钮点击都要记录对应网站 ID 的最近访问，隐藏网站则一律不记录。键盘选择卡片后按 Enter 只聚焦第一个操作按钮，不直接打开链接。
- 搜索必须覆盖实际按钮名称和 URL，因此单按钮卡片应能搜索“点击进入”，单 URL 双按钮卡片还应能搜索“暂无”。最近访问视图必须保留对应的单按钮或双按钮形态。
- 单按钮卡片与双按钮卡片在电脑、平板和手机上统一使用 136px 最小高度，不得因按钮数量、名称或描述长度不同而造成明显高低差。描述区域固定预留两行高度，一行不够时自动换行；新增或修改数据时必须把描述精简到手机、平板和电脑均能在两行内完整显示，省略号只作为异常内容的安全回退，不能作为正常卡片的展示方式，也不能重新压缩成单行。
- 所有卡片统一使用苹果式横向视觉结构：左侧是明显大于文字的分类图标，中间为左对齐的名称与两行描述，名称和描述作为整体垂直居中于图标右侧，右侧为独立操作列。桌面默认每行三张、平板每行两张、手机每行一张；单按钮在右侧垂直居中，双按钮在手机、平板和电脑上均在右侧上下等宽排列。桌面图标为 64×64px、名称 18px、描述 15px、操作列宽 92px；平板和手机图标为 60×60px、名称 17px、描述 14px、操作列宽 88px。名称使用明显粗于描述的字重；描述使用清晰、不过淡的次级文字色，不得过小或压缩回单行；不得恢复图标、文字和按钮上下散落的大面积留白布局。卡片四周边缘必须保持一致，不得使用只向下偏移的白色或灰色高光阴影形成独立底线；层次感由完整边框与卡片底色提供。
- 顶部导航、分类栏和卡片操作按钮统一使用轻量 Liquid Glass 视觉：浅色与深色模式分别使用半透明材质、细高光边缘和柔和阴影；顶部配色、明暗和菜单三个方形按钮必须使用四周等宽的内高光，不得只增强顶部边缘。展开的移动端导航面板以及“首页 / 最近访问 / 关于”菜单项必须具有足够遮盖度，不能让页面分类文字明显透出而影响辨识。只有顶部导航及分类栏使用背景模糊，卡片操作按钮仅用分层渐变模拟玻璃，避免大量模糊层影响手机滚动性能。手机端必须降低模糊强度，不支持 `backdrop-filter` 或启用降低透明度偏好时必须回退到高不透明度背景；卡片主体继续保持清晰、稳定的现有样式，不扩展为重度玻璃材质。
- 操作按钮默认使用带细微顶部高光的当前主题色玻璃填充和弱边框，默认柔光必须低调并以按钮中心向四周均匀扩散，不得使用向下偏移形成底部发光的阴影；悬停或键盘聚焦时才均匀增强当前主题色柔光。网站不提供“我的常用”、卡片收藏星标、收藏排序或 `sakura-favorites` 本地存储功能，不得在普通卡片或隐藏卡片中恢复。由于卡片主体不可点击，电脑悬停或手机按压卡片主体时，整张卡片的边框、背景与阴影必须保持稳定，不得闪亮、上浮、缩放或制造整卡可点击的错觉；仅卡片内的分类图标可轻微倾斜、上移并放大，离开或松手后柔和复位，启用 `prefers-reduced-motion` 时必须保持静止。
- 网站卡片、分类图标底座、顶部方形按钮、菜单与内容面板统一使用轻度苹果连续 R 角：电脑端卡片与主要面板采用 24px 回退圆角，手机和平板采用 22px，方形控制与图标按尺寸使用较小圆角；支持 `corner-shape: squircle` 的浏览器启用连续曲线，不支持时保留稳定的 `border-radius` 回退。分类栏、卡片操作按钮、计数器等胶囊元素继续使用完整圆弧，不得改成方形 R 角。圆角调整不得裁切边框、阴影、文字、焦点轮廓或点击区域。
- 普通单按钮卡片仍按普通网站规则核实链接并参与链接健康检查，不得因为采用按钮形态而跳过；用户指定的双按钮卡片和新世界卡片继续遵循各自的不访问、不联网验证规则。
- 用户指定的双按钮卡片 URL 视为已由用户人工确认：新增和以后修改此类卡片时，均不访问目标地址，不进行联网可用性、跳转、名称或内容验证，也不把该卡片的任一 URL 加入链接健康检查；仍须执行本地必填字段、HTTP(S) 基本格式及 ID、名称、URL 重复检查。
- 禁止添加 `recent: true` 或独立 `icon` 字段；网站卡片根据 `category` 自动继承所属分类的本地 Font Awesome Solid 图标，分类图标缺失时使用 `fa-link` 回退。
- `featured` 与 `popular` 视图已经停用；任何普通或隐藏网站对象都不得再添加这两个字段。

新增网站只需填写合法且已存在的 `category`，不寻找、下载或生成网站 Logo，不请求 favicon，也不需要修改 `sakura-app.js`、CSS 或分类对象的图标。除非用户明确要求修改整个板块图标，否则普通收录不得修改分类 `icon`。新增后，卡片在默认分类、搜索和最近访问视图中都必须继承所属分类图标，并与同板块其他卡片保持一致。

普通收录不得修改其他网站或其顺序、板块顺序、页面布局与样式、搜索、最近访问、筛选、README、GitHub Actions、Cloudflare 或部署配置。

## 删除网站

删除前根据用户提供的网站名称、网址、域名、条目 ID 和所属板块准确定位，并检查当前名称、ID、网址、分类、名称相近但不同的网站、镜像或备用域名、同一服务的其他独立条目及仓库中的功能性引用。

- 无法唯一确认时，不修改、不提交；列出所有可能匹配项等待用户确认。
- 目标已经不存在时，不修改、不创建空提交，并告诉用户未找到。
- 唯一确认后，完整删除目标数据对象，正确处理相邻逗号，不留下空对象、孤立逗号、语法错误或与目标相关的无效功能性引用。
- 不修改其他网站或顺序，不自动删除所属板块，不自动添加替代网站，不改变最近访问的 localStorage 结构。
- 前端会自动过滤已删除网站的无效最近访问记录，无需额外迁移。

## 新增板块

开始前确认板块显示名称、分类 ID、插入位置、图标和是否立即收录网站。

- 用户提供 ID 时检查其唯一性与格式，不擅自改成其他 ID。
- 用户未提供 ID 时，依据名称选择简短、清晰、唯一的英文小写 ID，只使用小写字母、数字和连字符；不得与现有分类 ID、网站 ID 或其他关键标识冲突。
- 新板块的分类对象必须配置仓库当前本地支持的 Font Awesome `icon`，类名使用现有 `fa-*` 格式并默认选择 Solid 图标；用户未提供时，从已安装图标中选择合适项。
- 不引入远程图标库，不使用目标网站 favicon，也不为板块内每个网站添加独立 `icon`。新板块下的全部卡片自动继承分类图标，缺失有效图标时前端使用 `fa-link` 回退。
- 板块标题图标与板块卡片图标必须一致；修改分类 `icon` 意味着该板块所有卡片同时变化，不需要逐个修改网站对象。
- 用户未说明插入位置时，不随意放置；先建议位置并等待确认。
- 按当前格式修改分类数组，不改变其他分类名称、ID、图标或顺序。
- 不自动向新板块添加网站，不为填充板块而推荐或收录网站。
- 检查桌面端、移动端分类导航和筛选是否自动支持新板块，检查验证脚本是否存在固定白名单。
- 仅在确有依赖时对固定分类代码做最小修改，并同步更新本文件的板块对应关系。
- 仅在分类结构说明确实受影响时更新 README。
- 若新增板块造成布局拥挤、溢出或按钮异常，只做支持新板块所需的最小适配，并在最终回复说明原因。

## 删除板块

删除前必须检查板块内是否存在网站。存在网站时，用户必须明确选择：

1. 移动到其他指定板块；
2. 删除板块并删除其中全部网站；
3. 暂不执行，等待逐个处理。

若用户未说明处理方式，不修改、不提交；列出板块内网站并请用户选择。

移动网站时必须确认真实存在的目标板块，只修改这些网站的 `category`，不修改名称、网址、描述、关键词或 `addedAt`。除非用户明确要求，不将网站移到含义明显不匹配的板块。

连同网站删除时，执行前列出将删除的网站；若用户要求先确认，确认前不修改。只完整删除目标板块中的网站对象，不误删其他板块网站。

删除分类时还必须：

- 从分类数组删除对应对象，不留下无效分类 ID。
- 搜索分类 ID、名称和图标的当前功能性引用，只删除确属该分类的引用。
- 不删除含义相同但与分类无关的普通文本。
- 不调整其他板块顺序，不删除相邻板块，不新增替代板块。
- 同步更新本文件的对应关系，并检查验证脚本固定规则、页面筛选、URL 参数和默认分类回退。
- 即使删除最后一个或唯一一个某类型板块，也不顺便重构分类系统。

## 板块改名或修改 ID

- 板块改名或修改 ID 不是普通文字替换；先检查所有使用该名称或 ID 的数据与逻辑。
- 只改显示名称时原则上保留原 ID。
- 修改 ID 时必须同步更新该板块下所有网站的 `category`、验证脚本、URL 分类参数兼容和本文件。
- 不擅自修改现有分类 ID；只有用户明确要求时才执行。
- 修改后不得遗留旧 ID 的功能性引用，但不需要改写 Git 历史。

## 特殊保护规则

- 网站卡片自动继承所属分类的本地 Font Awesome Solid 图标，同一分类下的卡片必须使用同一个分类图标；新增网站只需填写 `category`，不得为单个网站恢复独立 `icon` 字段。
- 不请求目标网站 favicon，不使用 Google favicon 或其他远程图标服务，不恢复 `assets/images` 下旧的 `logos` 网站图标目录。分类图标缺失时使用本地 `fa-link` 回退。
- 卡片图标的颜色、背景和边框通过现有 CSS 主题变量自动适配浅色与深色模式；不在网站数据中保存两套主题图标，也不为不同主题配置远程图片。
- iOS专区分类继续使用通用手机图标 `fa-mobile-alt`，不使用 `fab fa-apple` 或 Apple Logo；未经用户明确要求，不将分类图标改为任何品牌 Logo。
- 普通网站收录或删除不得修改分类图标。只有用户明确要求修改整个板块图标时才允许修改分类对象的 `icon`，修改后同板块全部卡片自动同步；不得为了单个网站而修改整个板块图标。
- 不修改 SAKURA 樱花品牌系统，但网站卡片不再固定使用樱花。根目录 `sakura-icon.png` 是唯一品牌图标母版；顶部和页脚品牌标志、浏览器 PNG favicon、Apple Touch Icon、PWA 192/512 图标及 Open Graph 分享资源都必须从该母版导出并保持同一造型。
- 普通网站收录或删除不得修改页面布局、CSS、搜索、最近访问、筛选、README、GitHub Actions 或 Cloudflare 配置。
- 不擅自修改 HTTP 网站地址；只有确认 HTTPS 长期稳定且当前任务明确要求更新时才可修改。
- `ios-update-blocker` 的 `.mobileconfig` 直链是有意设计。除非用户明确要求，不修改其网址、名称、描述或打开方式，不增加确认页、中间页、下载代理或跳转处理。
- 不根据链接健康报告自动删除网站。403、TLS、Cloudflare、超时和地区限制均需人工确认。
- 不关闭 TLS 证书验证，不使用不安全的 SSL 配置。
- 不自动修改 Cloudflare、DNS、自定义域名或部署设置。
- 不引入框架、数据库、后端、分析统计或 Service Worker，除非用户明确要求。

## 验证规则

普通数据或板块修改完成后至少运行：

```bash
python tools/validate_site.py
git diff --check
```

修改主题、搜索、筛选、最近访问或其他前端 JavaScript 逻辑时，还必须运行：

```bash
node --check assets/js/theme-init.js
node --check assets/js/app-guard.js
node --check assets/js/sakura-core.js
node --check assets/js/sakura-app.js
node --test tools/test_frontend.js
```

修改后台、数据库、Worker、Cloudflare 配置或前台数据加载时，还必须运行：

```bash
node --check assets/js/data-loader.js
node --check admin/admin.js
node --check worker/index.mjs
node --test tools/test_worker.js
python tools/validate_admin.py
```

- `validate_site.py` 必须为 0 个错误；已有 HTTP 提示不等于错误，不顺便修改无关 HTTP 网站。
- `tools/test_frontend.js` 必须全部通过；新增可复用的纯逻辑时应同步补充回归测试。
- `git diff --check` 必须无输出。
- 最终 diff 只能包含当前任务所需修改，不得含报告、缓存、临时文件或其他无关内容。

新增网站还要确认：

- 新网站出现在指定分类，`NEW` 标记、名称、域名核心词和关键词搜索均正常，ID 和 URL 不重复，卡片打开正确网址。
- 新网站卡片显示所属分类的图标，与同板块其他卡片和板块标题图标一致；搜索和最近访问等混合视图仍按网站所属分类显示图标。
- 网站对象没有独立 `icon` 字段，没有请求目标网站 favicon，没有新增远程图标依赖；浅色和深色主题下图标均正常显示。
- 顶部、页脚、favicon、PWA 和分享资源仍保留 SAKURA 樱花品牌标志。
- 卡片交互还要确认：单按钮和双按钮卡片主体点击均不跳转；单按钮固定为“点击进入”且地址等于 `url`；双按钮恰好两个且名称、地址正确，只提供一个 URL 时第二按钮必须为“暂无”并指向站内 `404.html`；键盘选择后聚焦第一个按钮；普通网站卡片按钮记录同一网站 ID，隐藏卡片按钮不记录；搜索和最近访问中保持正确按钮形态；卡片使用大图标、中间清晰分层的名称与两行描述、右侧操作列的横向对齐结构并保持统一高度，双按钮在手机、平板和电脑上均上下等宽排列且全部为胶囊形状；页面不得出现“我的常用”、卡片收藏星标或收藏排序控件，卡片主体悬停不位移；双按钮整卡继续跳过链接健康检查，普通单按钮仍参与；手机、平板、电脑及浅色、深色主题下无横向溢出。

删除网站还要确认：目标条目已不存在；搜索名称、ID 和域名不再显示；其他网站数据未变；分类和前端数据正常加载。

新增板块还要确认：位置、名称、唯一 ID 和本地 Font Awesome Solid 图标正确；板块标题与该板块全部卡片使用同一图标，缺失图标时 `fa-link` 回退有效；桌面和移动端显示正常；筛选只显示对应网站；URL 分类参数正常；空板块不导致脚本错误；本文件已同步。

删除板块还要确认：导航中已消失；没有网站引用旧 ID；板块内网站已按要求移动或删除；其他板块顺序不变；旧 URL 参数不会导致页面异常；本文件已同步。

只有板块修改影响页面布局时才做必要的本地页面检查；普通网站增删不擅自修改视觉样式。

## 提交规则

验证通过后只创建本地提交：

- 新增网站：`Add [网站正式名称] to navigation`
- 删除网站：`Remove [网站正式名称] from navigation`
- 新增板块：`Add [板块英文或正式名称] category`
- 删除板块：`Remove [板块英文或正式名称] category`
- 板块改名：`Rename [原板块名称] category`
- 其他维护：使用简短、准确的英文提交信息。

没有实际修改时不创建空提交；不提交无关文件；不执行 push。提交后检查 `git status`，最终回复说明修改文件、验证结果、提交哈希、提交信息和是否执行 push。

## 简短任务的默认理解

- “按网站收录要求把某网址加入某板块”表示自动执行本文件的完整核实、查重、字段、验证和提交规则。只把网站写入指定分类，不添加 `featured`、`popular` 或独立 `icon`，不寻找 Logo、不下载 favicon、不修改卡片渲染代码；卡片自动继承该分类图标，验证后只创建本地提交且不执行 push。例如收录到“在线工具”时使用 `tools`，新卡片自动继承 `fa-tools`。
- “按网站删除要求删除某网站”表示先唯一定位，再执行完整删除、验证和提交规则。
- “按板块维护要求新增某板块”表示执行新增板块规则，并同步更新本文件的分类对应关系。
- “按板块维护要求删除某板块并移动网站”表示先确认目标板块，再仅迁移分类并删除板块，同时更新本文件。
- 删除板块但未说明板块内网站如何处理时，必须停止修改并询问，不得自行删除或移动。
