# SAKURA 导航仓库维护规范

## 作用域与优先级

本文件适用于整个仓库。当前用户任务中的明确要求优先于本文件；但不得擅自放宽准确查重、目标确认、安全核实、必要验证、不提交无关文件和未经允许不推送等规则。

## 仓库基本规则

- 本项目是纯静态个人导航站，使用 HTML、CSS 和原生 JavaScript，不包含后端或数据库。
- 板块和网站数据集中存放在 `assets/js/sites-data.js`。
- 普通网站收录或删除原则上只修改 `assets/js/sites-data.js`。
- 板块新增、删除、改名或修改 ID 通常需要修改 `assets/js/sites-data.js`，并同步检查依赖分类数据的页面逻辑、验证脚本和本文件。
- 执行任务前先确认仓库为 `XHSCF/sakura-nav`、当前分支为 `main`，并检查工作区状态。
- 保留用户或其他任务产生的无关修改；不覆盖、还原、暂存或提交它们。
- 不擅自重构、格式化、重排、清理或顺手优化无关代码，不扩大任务范围。
- 不创建新分支或 Pull Request。未经用户明确要求，不执行 `git push` 或其他远程写入。
- 修改完成并验证通过后只创建本地提交，由用户通过 GitHub Desktop 推送。
- 没有实际修改时不创建空提交。
- 不提交 `link-health-report.md`、缓存、临时文件或其他自动生成文件。

## 当前板块对应关系

- 在线影视 → `movies`
- 动漫番剧 → `anime`
- 资源下载 → `downloads`
- 在线工具 → `tools`
- iOS 相关 → `ios`
- 游戏专区 → `games`
- 软件专区 → `software`

旧的 `ppt` 分类参数仅作为 `software` 的 URL 兼容别名保留；新增或修改网站数据时必须使用 `software`，不得继续写入 `ppt`。

收录网站时必须使用用户指定板块，不自行改换或创建分类，也不擅自修改分类名称、ID、图标或顺序。用户未指定板块且无法可靠判断时，停止修改并请用户确认。板块新增、删除、改名或修改 ID 后，必须同步更新本节，并确保它与 `assets/js/sites-data.js` 的分类数组一致。

## 收录新网站

### 核实与查重

新增前必须核实：

- 网站当前可访问性、正式名称和主要用途。
- HTTP 是否跳转到 HTTPS，是否发生域名跳转，以及最终稳定网址。
- 网址是否包含可删除的无意义追踪参数。
- 仓库是否已有相同网站、相同域名、旧域名、镜像域名或最终跳转地址。
- 名称相似的网站是否确实为不同服务。

若已收录，不重复添加、不修改原条目、不创建空提交，并向用户说明现有名称、网址和分类。若域名停放、跳转到无关内容、完全无法核实或存在明显恶意、诈骗或安全风险，不添加、不提交并说明原因。

不能仅凭单次出现 403、Cloudflare 验证、反爬、地区限制、超时、TLS/证书异常或 HEAD 被拒绝认定网站失效；这些情况需要谨慎复核。

### 数据格式

遵循 `assets/js/sites-data.js` 当前缩进、顺序和代码风格。基本字段为：

```js
{
  id: "example",
  name: "网站正式名称",
  url: "https://example.com/",
  description: "一句简短准确的中文描述。",
  category: "tools",
  keywords: ["网站名称", "英文名称", "域名核心词", "主要用途"],
  addedAt: "YYYY-MM-DD"
}
```

- `id`：简短、唯一、稳定，只使用英文小写字母、数字和连字符。
- `name`：使用当前正式名称，不添加“官网”“最新地址”等宣传词。
- `url`：使用核实后的稳定地址，优先 HTTPS，删除无意义追踪参数。
- `description`：一句简短、客观的中文说明，风格与现有条目一致。
- `category`：严格使用用户指定板块对应的分类 ID。
- `keywords`：包含名称、常用缩写、域名核心词和主要用途，不堆砌无关词。
- `addedAt`：使用实际收录日期，必须是合法的 `YYYY-MM-DD`。
- 禁止添加 `recent: true` 或独立 `icon` 字段；卡片继续使用统一的本地樱花图标。
- 只有用户明确要求“站长推荐：是”时添加 `featured: true`。
- 只有用户明确要求“热门网站：是”时添加 `popular: true`。
- 用户未说明或明确填写“否”时，不添加对应布尔字段。

普通收录不得修改其他网站或其顺序、板块顺序、页面布局与样式、搜索、收藏、最近访问、筛选、README、GitHub Actions、Cloudflare 或部署配置。

## 删除网站

删除前根据用户提供的网站名称、网址、域名、条目 ID 和所属板块准确定位，并检查当前名称、ID、网址、分类、名称相近但不同的网站、镜像或备用域名、同一服务的其他独立条目及仓库中的功能性引用。

- 无法唯一确认时，不修改、不提交；列出所有可能匹配项等待用户确认。
- 目标已经不存在时，不修改、不创建空提交，并告诉用户未找到。
- 唯一确认后，完整删除目标数据对象，正确处理相邻逗号，不留下空对象、孤立逗号、语法错误或与目标相关的无效功能性引用。
- 不修改其他网站或顺序，不自动删除所属板块，不自动添加替代网站，不改变收藏或最近访问的 localStorage 结构。
- 前端会自动过滤已删除网站的无效收藏和访问记录，无需额外迁移。

## 新增板块

开始前确认板块显示名称、分类 ID、插入位置、图标和是否立即收录网站。

- 用户提供 ID 时检查其唯一性与格式，不擅自改成其他 ID。
- 用户未提供 ID 时，依据名称选择简短、清晰、唯一的英文小写 ID，只使用小写字母、数字和连字符；不得与现有分类 ID、网站 ID 或其他关键标识冲突。
- 用户未提供图标时，从项目已安装并支持的 Font Awesome 图标中选择合适的本地图标类，不引入远程图标库或 favicon。
- 用户未说明插入位置时，不随意放置；先建议位置并等待确认。
- 按当前格式修改分类数组，不改变其他分类名称、ID、图标或顺序。
- 不自动向新板块添加网站，不为填充板块而推荐或收录网站。
- 检查桌面端、移动端分类导航和筛选是否自动支持新板块，检查验证脚本是否存在固定白名单。
- 仅在确有依赖时对固定分类代码做最小修改，并同步更新本文件的板块对应关系。
- 仅在分类结构说明确实受影响时更新 README。
- 若新增板块造成布局拥挤、溢出或按钮异常，只做支持新板块所需的最小适配，并在最终回复说明原因。

## 删除板块

删除前必须检查板块内是否存在网站。存在网站时，用户必须明确选择：

1. 移动到其他指定板块；
2. 删除板块并删除其中全部网站；
3. 暂不执行，等待逐个处理。

若用户未说明处理方式，不修改、不提交；列出板块内网站并请用户选择。

移动网站时必须确认真实存在的目标板块，只修改这些网站的 `category`，不修改名称、网址、描述、关键词、`addedAt`、`featured` 或 `popular`。除非用户明确要求，不将网站移到含义明显不匹配的板块。

连同网站删除时，执行前列出将删除的网站；若用户要求先确认，确认前不修改。只完整删除目标板块中的网站对象，不误删其他板块网站。

删除分类时还必须：

- 从分类数组删除对应对象，不留下无效分类 ID。
- 搜索分类 ID、名称和图标的当前功能性引用，只删除确属该分类的引用。
- 不删除含义相同但与分类无关的普通文本。
- 不调整其他板块顺序，不删除相邻板块，不新增替代板块。
- 同步更新本文件的对应关系，并检查验证脚本固定规则、页面筛选、URL 参数和默认分类回退。
- 即使删除最后一个或唯一一个某类型板块，也不顺便重构分类系统。

## 板块改名或修改 ID

- 板块改名或修改 ID 不是普通文字替换；先检查所有使用该名称或 ID 的数据与逻辑。
- 只改显示名称时原则上保留原 ID。
- 修改 ID 时必须同步更新该板块下所有网站的 `category`、验证脚本、URL 分类参数兼容和本文件。
- 不擅自修改现有分类 ID；只有用户明确要求时才执行。
- 修改后不得遗留旧 ID 的功能性引用，但不需要改写 Git 历史。

## 特殊保护规则

- 不为单个网站恢复独立 `icon` 字段，不恢复远程 favicon，不修改统一樱花图标系统。
- 普通网站收录或删除不得修改页面布局、CSS、搜索、收藏、最近访问、筛选、README、GitHub Actions 或 Cloudflare 配置。
- 不擅自修改 HTTP 网站地址；只有确认 HTTPS 长期稳定且当前任务明确要求更新时才可修改。
- `ios-update-blocker` 的 `.mobileconfig` 直链是有意设计。除非用户明确要求，不修改其网址、名称、描述或打开方式，不增加确认页、中间页、下载代理或跳转处理。
- 不根据链接健康报告自动删除网站。403、TLS、Cloudflare、超时和地区限制均需人工确认。
- 不关闭 TLS 证书验证，不使用不安全的 SSL 配置。
- 不自动修改 Cloudflare、DNS、自定义域名或部署设置。
- 不引入框架、数据库、后端、分析统计或 Service Worker，除非用户明确要求。

## 验证规则

普通数据或板块修改完成后至少运行：

```bash
python tools/validate_site.py
git diff --check
```

- `validate_site.py` 必须为 0 个错误；已有 HTTP 提示不等于错误，不顺便修改无关 HTTP 网站。
- `git diff --check` 必须无输出。
- 最终 diff 只能包含当前任务所需修改，不得含报告、缓存、临时文件或其他无关内容。

新增网站还要确认：指定分类和“最近收录”正常显示；名称、域名核心词和关键词可搜索；卡片打开正确网址并继续使用统一樱花图标；ID 和 URL 不重复。

删除网站还要确认：目标条目已不存在；搜索名称、ID 和域名不再显示；其他网站数据未变；分类和前端数据正常加载。

新增板块还要确认：位置、名称、ID、图标正确；桌面和移动端显示正常；筛选只显示对应网站；URL 分类参数正常；空板块不导致脚本错误；本文件已同步。

删除板块还要确认：导航中已消失；没有网站引用旧 ID；板块内网站已按要求移动或删除；其他板块顺序不变；旧 URL 参数不会导致页面异常；本文件已同步。

只有板块修改影响页面布局时才做必要的本地页面检查；普通网站增删不擅自修改视觉样式。

## 提交规则

验证通过后只创建本地提交：

- 新增网站：`Add [网站正式名称] to navigation`
- 删除网站：`Remove [网站正式名称] from navigation`
- 新增板块：`Add [板块英文或正式名称] category`
- 删除板块：`Remove [板块英文或正式名称] category`
- 板块改名：`Rename [原板块名称] category`
- 其他维护：使用简短、准确的英文提交信息。

没有实际修改时不创建空提交；不提交无关文件；不执行 push。提交后检查 `git status`，最终回复说明修改文件、验证结果、提交哈希、提交信息和是否执行 push。

## 简短任务的默认理解

- “按网站收录要求把某网址加入某板块”表示自动执行本文件的完整核实、查重、字段、验证和提交规则；未明确推荐或热门时均按“否”处理。
- “按网站删除要求删除某网站”表示先唯一定位，再执行完整删除、验证和提交规则。
- “按板块维护要求新增某板块”表示执行新增板块规则，并同步更新本文件的分类对应关系。
- “按板块维护要求删除某板块并移动网站”表示先确认目标板块，再仅迁移分类并删除板块，同时更新本文件。
- 删除板块但未说明板块内网站如何处理时，必须停止修改并询问，不得自行删除或移动。
